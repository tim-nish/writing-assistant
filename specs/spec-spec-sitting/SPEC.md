---
id: SPEC-spec-sitting
relocated: true
---

> **Relocated — pointer, not a copy.** `SPEC-spec-sitting` is a
> **personal-toolkit command spec** (`/spec-sitting`), not a writing-assistant
> feature. It was mis-filed here under issue #483; its real home is the
> **claude-toolkit** repo, alongside `spec-triage-gh` and the other command
> specs. The full contract and the command live there:
>
> - Spec: `claude-toolkit/specs/spec-spec-sitting/SPEC.md`
> - Command: `claude-toolkit/commands/spec-sitting.md`
>
> This breadcrumb exists only because #483/#487/#488 were opened on
> writing-assistant. **Do not restore the full spec here** — it would drift from
> the canonical copy in claude-toolkit (pointer-not-copy).
