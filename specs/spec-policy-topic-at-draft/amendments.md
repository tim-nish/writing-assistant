
# SPEC-policy-topic-at-draft — ratified amendments

Companion to `SPEC.md`: the dated, ratified amendment record for this spec.
New amendments append here, newest-last.

> **Amended 2026-08-01 (triage, #1144; coupled with #1140/#1142) — for a BRIEF-CARRYING run the policy-topic set is DERIVED from the brief's members and rendered as a DISCLOSURE; the human gate survives only for a cold run, and either way the surface enters the gate registry.** CAP-2's two-step topic selection (`SPEC.md:38-50`) dates from #230, **before the Brief existed** and before the Strand/thread-line vocabulary was ratified. On 2026-08-01 it fired inside a run whose brief already carried five selected Strands with their pins, and asked the owner to choose policy topics for material the brief had already fixed. The owner's analysis, carried in substance: once the members are in the brief, the related topics are reverse-resolvable from them, and **no reason is stated anywhere for why the interview's seed material must be Topics** rather than the Strands the owner selected. **Two grounds, and only one of them is served.** The boundary test is covered — *"would changing this fact change the ARGUMENT, or only how the argument is RENDERED?"* (owner decision record — 2026-07-31 (article design boundary)) — and a topic selection that seeds which recorded positions may raise a tension is argument-side, so it belongs at the Brief or is derived from it, never re-asked at realization. The **Topics-vs-Strands** half is **uncovered**: `policy_lookup` returned no served position discriminating whether the interview's seed material must be Topics, and this amendment records that miss rather than implying a position. So the derivation is this consumer's decision, taken on the boundary test alone. **What is decided:** for a run carrying a brief, the topic set is derived from the members (Strand → lesson → served topic), rendered to the owner as a **disclosure naming which topics were derived and from which members**, and the ≤2 cap and the code-enforced whitelist are **unchanged** — a derivation may not widen what a read may reach. For a run with no brief the two-step gate stands exactly as CAP-2 states it. **The registry half binds both branches:** the surface has no entry in `draft_gates.GATES`, so it sits outside the payload audit and the pending-decision map that #1132 and story 20.117 built — an owner screen outside the registry contradicts *a run's gates are auditable*. It gains an entry whichever branch it takes; a disclosure that is not a gate is declared with `owner_decision: None`, which is the shape `resume-confirmation` already uses. **Overturn condition, stated because the uncovered half is real:** a cold run in which the derived set would have been wrong — a brief whose members' topics do not cover the tension worth raising. That is the observation this decision waits on, and it would reopen the derivation rather than the disclosure. Delivery is story 20.127, which carries this beside the interview changes because both land in `scripts/draft-pipeline.py` and `skills/draft-article/stages/stage2.md`.

> **Amended 2026-08-01 (triage, #1168) — the DERIVATION half of the amendment above is RETRACTED on a falsified premise; the REGISTRY half stands, delivered. CAP-2's two-step gate is the only branch, for every run.** The amendment above decides that "the topic set is derived from the members (**Strand → lesson → served topic**)". That join does not exist and the served surface rules against building one. **Measured, not assumed** (story 20.127, #1152, the delivery attempt): against the newest real brief — 5 members, terrain run `20260801T002735-440395` — every member's served `topic` is the synthetic bucket `hub-lessons`, never a `topics/*.md` file, while `read-policy-source.py list-topics` serves 9 topic names; the intersection is **empty**. **Consult COVERED and DISCRIMINATING** (owner decision record — 2026-07-27 (terrain topic/tag axis correction); owner decision record — 2026-07-28 (approach-per-axis)): *"no served Lesson→Topic join exists … Building a Lesson→Topic join to rescue the wording was declined"*, and *"screen 1 selects an APPROACH, each approach carries its own axis over its own population … Lessons-plus-Journeys on tags … **topic selection lives only inside the Decision flow**."* The second line goes **further than the missing join** and is why this is a retraction rather than a re-plumbing: a brief's members are Strands, so topic selection does not belong on that population at all. The amendment above named its uncovered half correctly (*"the Topics-vs-Strands half is uncovered"*) — but a `policy_lookup` run on **Topics-vs-Strands as an axis question**, rather than on the interview's seed material, returns the two lines quoted here. **The miss was in the query, not in the surface**, which is recorded because that is the reusable finding: an uncovered-half declaration is only as good as the question that failed to cover it. **What is retracted:** the derived-disclosure branch, its `owner_decision: None` registry entry, and the `Strand → lesson → served topic` derivation. **What stands:** the registry half, **delivered** at #1166 (`draft_gates.GATES["policy-topics"]`) — the cold-run selection is inside the payload audit and the pending-decision map, which was the durable half of the finding; and CAP-2's two-step gate (`SPEC.md:40-52`), unchanged, for brief-carrying and cold runs alike. **What is NOT decided here, deliberately:** the owner's original 2026-08-01 complaint — that a brief-carrying run asks for topics over material the brief already fixed — survives this retraction unanswered. Retiring topic selection in favour of Strand-seeded tension questions is a live alternative that was weighed and **declined at this gate on scope**: the #1144 gate ratified a *derivation*, and deciding a *retirement* on a retraction would settle a larger question than the gate that produced it put to the owner. **Reopen trigger:** an owner statement that Strand-seeded tension questions are the intended design — which reopens as its own spec sitting, not as an amendment to this one. Delivery is story 20.132, which carries only the code-comment correction this retraction falsifies (`scripts/draft_gates.py:205-213`, which currently asserts the amendment above ratifies a second branch).
