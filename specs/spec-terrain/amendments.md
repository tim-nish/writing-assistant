# SPEC-terrain — ratified amendments, 2026-07-30 onward

Companion to `SPEC.md` (listed in its `companions:` frontmatter): the dated,
ratified amendment blockquotes of SPEC-terrain, relocated verbatim per the
2026-07-27 amendment-history-companion decision (#829). **Subsequent
amendments append here, newest-last** — `SPEC.md` carries the pointer, never
the blocks.

Amendments dated **2026-07-24 through 2026-07-29** live in
`amendments-archive.md`, relocated verbatim on 2026-07-31 when this file
reached the 72,000-byte spec ceiling. That file is closed; nothing appends
to it.

> **Amended 2026-07-30 (triage, #933/#934)** per /triage-gh. Journey presence
> on a Strand row is **derived and marked by absence**, with the coverage
> denominator stated on every Strand-row screen. Three facts drove it. The
> flag never reached the renderer: the record-authoritative path composed a
> literal `False` on every entry while holding the shard pointer that answers
> the question, so a screen asserted that no Journey material fell under a tag
> carrying 49 paired journey records — a wrong-kind claim logged as success.
> Coverage inverted the marker's information content: the marker was designed
> against ~50% and the served manifest carries 109 journeys against 117
> lessons, so marking presence decorates nearly every row while the thin
> Strands are the actionable set the writability verdict already treats as
> such. And the retirement of the `J<n>` namespace (2026-07-28, #871) left the
> code that mints it standing — a `journey` counter and a `J` prefix branch
> reachable only on a kind the record path never emits, which is why a screen
> could be written as though `J` rows might appear. The denominator is the
> **load-bearing half**, not decoration: absence-marking is correct only while
> coverage stays high, so the denominator is what makes the next inversion
> visible on the screen rather than discovered late. Rejected: threshold-
> conditional presence marking (a magic number in the spec, and a marker that
> appears and disappears makes two screens non-comparable and every check over
> it conditional); dropping the marker entirely in favour of the writability
> verdict (smallest surface and no rot, but the owner then finds the thin rows
> by noticing the count is 49 rather than 51 and hunting — the glance signal
> is the marker's whole purpose).

> **Amended 2026-07-30 (triage, #935)** per /triage-gh. The 2026-07-28 #874
> location ruling is **narrowed to Terrain's owner-facing output**. The View
> stays in this repository, guarded by the committed ignore entry and the
> staged-artifact check; **run workspaces and debug artifacts resolve back to
> the machine state root**. Ground: the portfolio-wide class split places
> human-facing artifacts in the working repo and machine-readable
> intermediates, caches and resumable state in machine-state dirs (owner
> decision record — 2026-07-16 (artifacts live where the human works)), and
> #874 reached one clause past the deliverable. Observed cost of the wider
> form: roughly forty repo-key directories accumulated in a working tree with
> no GC, most of them `-tmp-*` leftovers from test and dogfood runs, deleted
> by hand. The split is drawn at the resolver, which already drew one — the
> draft pipeline's caches and checkpoints never moved — so a boundary
> relocates and no caller changes. #874's owed retention rule is discharged
> **by relocation rather than by GC**: growth was non-deferrable because the
> runs sat in a working tree. Two things stay undischarged and are named
> rather than assumed: the accumulation already on disk (a one-time deletion
> no code owns) and, until this lands, test runs keying a real repository.
> Rejected: keeping the location and writing the retention rule #874 owed (it
> discharges an existing obligation with a smaller spec surface, but leaves
> verbatim hub renderings and pins in a public tree defended by a guard rather
> than by absence, and contradicts the served class split); relocating the
> View too (publication risk to zero and two mechanisms deleted, but it loses
> the immediate-inspection affordance the owner stated, and a printed path is
> not a file already open in the tree).

> **Amended 2026-07-30 (triage, #936)** per /triage-gh. The composed group
> line's provenance is **declared once per surface, not once per line**. Where
> every line of a visual class is composed, the preamble's declaration
> discharges the obligation; a per-line marker is owed again when a screen
> mixes composed and quoted lines of the same visual class, and then it marks
> the **minority** class. The obligation is unchanged in force — only its
> carrier is bounded, and the no-silent-fallback rule (#850 D1) is untouched.
> Observed: the parenthetical appeared on all twenty group summary lines of a
> screen whose preamble had already declared the class, carrying zero
> information per line while costing attention on every one. The
> violation-layer rule is **satisfied, not weakened** (owner decision record —
> 2026-07-26 (carry a rule at its violation layer)): it requires the carrier
> to sit where the human acts, which the preamble does — same screen, same
> read — not one carrier per line. Rejected: keeping a per-line carrier in a
> shorter form (honours the violation-layer rule with no interpretive step and
> keeps the check unconditional, but a shorter constant is still zero bits per
> line, so it leaves the reported defect in place in a smaller font);
> inverting the polarity to mark quoted lines by their citations (the signal
> would then carry information and be verifiable, but an unmarked composed
> line is close to what the no-silent-fallback rule was written against, and
> that failure is silent rather than loud).

> **Amended 2026-07-30 (triage, #937/#939)** per /triage-gh. **Selection
> accepts a SET of Strand indexes, and the in-common claim is recomposed over
> exactly that set** at the brief gate, as a machine-composed proposal beside
> free-form override; the brief records the adopted claim, its member set,
> each member's served gloss and cite, and the pins. Single-Strand selection
> becomes the degenerate case rather than the only structured path. Observed
> divergence: an owner who had explored a tag and pointed at four co-tag
> sections (19 distinct Strands) was offered exactly two exits — free text, or
> one Strand as a spine with "the other 18 stay available as harvest material"
> — so no path composed from the selection, and the gate asked for a
> one-sentence summary in the owner's own words, which is raw-artifact
> homework. The ratified model says otherwise (owner decision record —
> 2026-07-29 (terrain draft handoff)): a claim composed over a member set is
> pinned to that set, a subset selection recomposes and re-offers it, and the
> brief records the adopted claim together with its members. **The
> no-second-proposer boundary is untouched, by its own test** — a combination
> becomes a proposal exactly when something other than the owner narrows the
> candidate set, and the owner did the narrowing here; the map still composes
> no narrative structures. Recording the member set is not bookkeeping: the
> completeness invariant follows the selected set into drafting, and with no
> members recorded, omission becomes silent — which is what that invariant was
> corrected to prevent. **The brief gate may additionally carry a coherence
> CONSULTANT (#939), bound by four rules and deliberately not by a
> procedure:** gate shape (proposals plus free-form override, nothing adopted
> silently); grounding (assessments and substitutions cite served renderings
> and group claims at the pin, never invented material); honesty (say plainly
> when a set does not cohere, disclose uncertainty rather than emitting a
> confident structure); and **no hiding** — a substitution proposal enumerates
> its candidates, because offering the best swap while discarding weaker ones
> is ranking, which falls on the far side of the boundary while merely adding
> unselected material does not. The consultant is diagnostic over an
> owner-selected set and never runs upstream of a selection. Rejected:
> shipping the selection half now and routing the substitution boundary to a
> hub conversation (the issue itself invites that, and the consultant would be
> specified against an observed failure instead of an imagined one — but the
> brief gate would ship with no coherence assessment, so an incoherent set
> freezes into a brief and the cost lands in drafting); recomposing at the
> gate while leaving the brief a plain string (much the smallest change and
> nothing downstream learns a new shape, but the completeness invariant then
> has no member set to follow and omission becomes silent).

> **Amended 2026-07-30 (triage, #938)** per /triage-gh. The owner may pull a
> **Full Report for named group ids**, rendering per group, in the order
> asked: the group's existing composed claim verbatim, then every member
> Strand's full served rendering — gloss, deterministic context line, journey
> arc — in prose. The compact all-groups form shows member ids plus a claim,
> which is enough to navigate and not enough to judge whether a grouping makes
> sense; the ratified form anticipated the report (owner decision record —
> 2026-07-29 (terrain draft handoff)). Four constraints fix what it is not: it
> **preserves** claims and never recomposes (the claim renders over the
> unchanged full member set it was composed from, so it stays true to its pin;
> recomposition belongs to subset selection, and inspection and selection stay
> different acts); it **selects nothing**; it renders from **held state**,
> never by reading the written rendering back; and it **restates the pin and
> the group definitions**, since group ids are per-screen, per-pin identifiers
> that do not survive a re-run. It **relays whole**, a stated exception to the
> size switch rather than an oversight — reading each group entire is the
> report's purpose — bounded by the owner's own pointers, covering the groups
> named and never the whole member. Rejected: extending the per-invocation
> View file (reuses a ratified artifact and has no screen-budget question, but
> answers a mid-conversation request by rewriting a file and handing over a
> path, moving the reading outside the interaction where the judgment
> happens); a relay obligation in prose with no code path (cheapest, and the
> material is already in the payload — but prose binding a model is advisory
> at the layer where it breaks, and the reported defect *was* a relay doing
> its best, flattening nineteen Strands into headline one-liners).

> **Amended 2026-07-30 (triage, #941)** per /triage-gh. **CAP-3 is relocated
> verbatim to the companion `presentation.md`**; `SPEC.md` carries a pointer
> that *names* what moved rather than deferring to a lookup, and this record
> stays the single dated history for the whole spec — one spec, one amendment
> file, including for CAP-3's own clauses. Trigger: the byte ceiling fired
> (80,902 against 72,000) after five amendments landed in one sitting on a file
> that had been 71,264 bytes — 736 bytes under the ceiling — so the next
> normative clause of any size was going to trip it. **The growth was measured
> before anything moved**, which is the ordering the standing rule requires:
> ship the ceiling, hold restructure until the ceiling produces a breakdown of
> which content classes dominate, because restructure claims the organisation
> is wrong while a ceiling only measures size (owner decision record —
> 2026-07-26 (ship the ceiling, hold restructure on its own evidence)). The
> breakdown: CAP-2 26,432 bytes (33%), CAP-3 24,587 (30%), open questions
> 13,937 (17%), CAP-4 6,149 (8%), preamble 3,044 (4%), CAP-1 2,426 (3%). Two
> capabilities were 63% of the file, and no large block of superseded prose
> existed to pointer-ify (six occurrences of historical/superseded markers
> corpus-wide in the file). **Nothing was compacted** — ratified text never is,
> and a projection may relocate text but never re-express it, so the move
> changed no clause. Blast radius verified rather than assumed: six inbound
> citations across four files, exactly one of them line-ranged
> (`spec-policy-editorial-direction/SPEC.md:50` → the preamble, unaffected),
> and **no check reads this spec's content at all**. Result: `SPEC.md` 57,560
> bytes, `presentation.md` 25,848, both under the ceiling. **Two alternatives
> rejected**, and one dismissed on arithmetic: splitting the open questions out
> as well (durable headroom at ~42 KB, but three companions for one spec is the
> larger restructure the ordering rule says to hold until measurement supports
> *how much* to move, which it does not); splitting into two peer specs at the
> assembly↔presentation seam (a peer spec states independence a companion only
> implies, and the citation cost was verified small — rejected because CAP-2
> already contains screen-level rules, so a spec boundary drawn there runs
> through a seam the existing text crosses, and every future amendment would
> face a which-file question whose wrong answer splits one contract silently);
> and relocating *only* the open questions, which leaves 5,035 bytes of
> headroom against ~9,600 bytes of amendments observed in a single sitting —
> a deferral wearing a fix's clothes. **Never available:** raising the ceiling
> or adding this file to the ratchet list. The check's own text forbids raising
> a ratchet to absorb growth, and an exemption minted for another file cannot
> be inherited unless the new member states the original justification in its
> own terms, which "five amendments landed at once" is not (owner decision
> record — 2026-07-27 (an inherited exemption signals nothing)).

> **Corrected 2026-07-30 (triage, #933)** per /triage-gh — a same-day
> correction of the #933/#934 amendment above, not a new decision. That
> amendment's first clause read *"the entry's `journey` field is **derived** —
> a paired journey record exists, i.e. the shard pointer is not null — and is
> never a literal"*, and it is **not implementable**: on the
> record-authoritative path `journey` is a **kind discriminator** ("this entry
> IS an arc rendering"), set from the served file path and consumed to split
> served renderings into the lessons and journeys lookups. Measured on the live
> corpus: deriving it from presence moves **109 of 117** lesson renderings out
> of the lessons lookup, which breaks the journey-shard enumeration (it
> iterates that lookup) and collapses the attachment count. The literal `False`
> there is correct. **Presence was never dropped in composition** — the paired
> record's pointer is set on every composed entry — so #933's mechanical claim
> is right about the symptom and wrong about the site: the gap is at the
> renderer, which never consumes it. The clause now binds **the observable and
> its source of truth**: presence is read from the paired journey *record*,
> never from a literal, and never inferred from a rendering path's
> addressability (117 lessons, 109 journey records, 109 paired, 109 pointers
> resolving — divergence 0 today, so the invariant is stated before it bites).
> Bullets two and three — absence-marking and the coverage denominator — are
> #934's decision and are **unchanged**. **The transferable half:** this spec
> names served-side and config vocabulary, and the failed clause named a
> consumer's private dict key using a word the spec already binds to a served
> record kind, so two questions collapsed into one token and a story
> implemented the wrong one faithfully. A spec clause that names an internal
> field can be wrong in a way no reader of the spec can detect and no check
> catches — **all four terrain checks passed with the misrouting change in
> place**, because none exercises the lessons/journeys split. Rejected:
> declaring a separate `has_journey` field in spec text (one carrier named
> once, and the name would document the distinction — but it re-commits the
> spec to naming internal shapes, which is the category of statement that just
> failed, and it adds redundant state a reader must arbitrate between); naming
> the shard pointer instead (smallest diff and correct today, but it conflates
> "a journey exists" with "its rendering was addressable", which is the same
> wrong-kind-claim class as the bug being fixed, silently wrong the first time
> the two diverge).

> **Amended 2026-07-30 (triage, #976/#977 — one decision, two issues) — screen 2 is COMPOSED BY THE SCRIPT end to end; the model supplies group claims and never retypes a Strand row.** The screen-2 flow asked the model to *"relay the returned `listing` as given … each Strand quoting its served rendering with its deterministic context line"* (`skills/terrain/steps/screens.md:57`), and the 2026-07-30 dogfood shows the relay is not faithful: L116 rendered in G8 as *"…is a harness gap, not a wording problem"* and in G9 as *"…is a harness gap"*, **both expanded rows**, so one was silently shortened; and L56's expanded row in G6 carried no `no-journey` mark although the screen header promised one and the compressed groups G12/G15 showed it. **Both are one defect, and neither is a code defect.** The rows are deterministic script output — the mark is added in `_strand_context_line`, which both expanded paths call — so the data was right in each case and the *relay* dropped or reworded it. What was missing is a carrier: "relay as given" is an obligation, violated by omission, with no event to gate and nothing making the absence visible, which is precisely the layer at which a rule stated in prose is enforced by whatever consideration is strongest when the moment arrives. **The remedy removes the opportunity rather than restating the instruction:** the model composes only the `in common:` claims and passes them back through the **existing** `--claims` round-trip (`screens.md:111`, whose path already *"carries them verbatim"*, `:118`), and the script emits the final screen — rows and claims together. The View and report paths are already script-composed end to end; this makes the at-or-under-budget screen-2 path match them, so a reworded headline and a dropped absence-mark both become structurally impossible rather than detectable after the owner has read the wrong line. **A bounded question the delivering story must answer rather than assume:** claim text now round-trips through a CLI argument, so quoting, embedded newlines and shell-safety are part of the contract — the View path may already have solved this, and the story checks that before inventing an encoding. **Not licensed:** permitting marked compression of an expanded row. That was the issue's own second horn and it is declined — a mark the same model must remember to add is enforced by exactly nothing that the verbatim rule was not already, so it would trade a silent violation for a differently-silent one while recording the weaker guarantee permanently. Delivery is story 20.66. **#977 was classified `story` at the batch gate and RECLASSIFIED to `spec` before any story was written**, because its criteria decide this same relay-fidelity invariant rather than a local rendering bug; the reclassification is recorded here rather than made silently.

> **Amended 2026-07-30 (triage, #979/#980 — one decision, two issues) — a compressed group GLOSSES each member on its first appearance, and a claim that cannot state a commonality SAYS SO; the proposed member cap is DECLINED on measurement.** The 2026-07-30 dogfood reports two group-level defects. The first is real and gets an obligation: compressed groups vary in whether they help, and G17 (`L21 · L22 · L70 · L77 · L83 · L95 · L117`) and G18 are bare index chains, so a reader entering there has no handle beyond the group's `in common:` line and must scroll back to recover each member's headline — while G12/G13/G15 already gloss members not yet seen. **That practice becomes a rule: every compressed group glosses each member on its FIRST appearance**, which costs nothing where the behaviour is already correct and closes the case where it is not. **The second is a premise correction.** The issue proposes a soft member cap because G10 (15 members) and G12 (13) "exceed what one in-common claim can honestly state" — but the ratified sectioning cap is **20% of PLACEMENTS** (`specs/spec-terrain/SPEC.md:254,278,284`), which for the reported 107 placements is `max(3, int(107 × 0.2)) = 21`. **Neither group was ever in breach**, and no second, tighter cap is licensed: this file already records that *"the future remedy if one is ever needed is a second navigation step, not a cap"* (the 2026-07-27 within-topic-cap decision, whose own decline states it was named in advance **so that a cap would not be invented under time pressure later** — *owner decision record — 2026-07-27 (no within-axis cap; a second navigation step)*; consulted at the pin, coverage partial, **covered** on this fork). Inventing one here would also re-introduce the ranking judgment the compact all-groups form was adopted to remove. **What the issue actually found is a COMPOSER signal, not a size signal:** G12's claim visibly trailing into an enumeration (*"…trials exercised, coverage explored, ignorance…"*) is the composer failing to find a commonality, so the honest response is disclosure — **the claim declares that it could not state a single commonality** — rather than resizing the group until the sentence reads better. Silent restructuring on a machine judgment about prose quality is the shape that turns grouping into a gate wearing navigation's clothes, and the existing cap is arithmetic for exactly that reason. **Watch trigger, this decision's own weakness as an observable:** degenerate-claim disclosures appearing on many groups at once would mean the grouping, not the composer, is wrong — at which point subdivision on that signal (the ratified second navigation step, already implemented) becomes right and this disclosure was only the instrument that showed it. Delivery is story 20.67.

> **Amended 2026-07-31 (triage, #986/#987 — one decision, two issues) — the Full Report relays UNTRUNCATED and names its journey label; the deterministic context line moves to a FOOTNOTE; the md export is DECLINED a second time.** The 2026-07-30 dogfood reports the Full Report truncating exactly the material it exists to show: every `how it changed:` line ends in `…` mid-sentence. **This was never a gap in the contract — it was a violation of it.** CAP-3 already binds *"it relays whole — a stated exception to the size switch below, not an oversight"*, while the renderer clipped every line to a fixed width (`VIEW_LINE_CHARS`), so in the one surface exempted from the size switch the journey arc — the thing screen 2 advertises 49 of 51 Strands as carrying — was the content systematically cut. Read with the same-day ruling that **UX defines correctness** (*owner decision record — 2026-07-27 (terrain cold-reader verdict)*; consulted at the pin, coverage partial, **covered** on this fork), a report the owner cannot read whole fails its purpose whether or not a clause forbade the clipping. The clause is honoured, not amended. **A legend is added** because the owner asked whether `how it changed:` shows journeys: it does, but nothing on the surface said so, while screen 2's legend defines a `J` row that never renders here — the ratified vocabulary and the rendered label disconnected on the reading surface. **The context line moves to an end-of-report footnote.** It bundles cross-group placement (which serves selection-screen navigation), the audit pin (which serves verification) and a completeness attestation with no reader action attached — three audiences, none of them the reader of one group read whole. It is **relocated, never dropped**: the pins stay reachable, so the report still restates what it rendered, and the selection screens keep the line on the row where it does serve navigation. **A premise correction the issue's second half rests on.** #987 reports the line present for every G4 member and only for L112 in G10, and argues that per-member presence with no rule is a defect either way. **Presence was never conditional.** `_strand_context_line` emits it for every Strand and only its first field varies — a Strand with no co-tags renders `in no other Topic`. G4 and G10 differ in **co-tagging**, not in row contract; the contract was already uniform, and this amendment changes where a uniform line lives rather than whether it exists. Recorded because acting on the reported shape would have "fixed" a rule that was not broken. **A latent check bug found at the same site and owed by the delivering story:** `check-terrain-report-inner.sh` asserts `body.count("(also in:") == len(strands)`, which fails on any co-tagless Strand and passes today only because every fixture Strand happens to be co-tagged — the assertion encodes the same wrong premise the issue did. **The md export is DECLINED a second time.** This file already records the rejection (*answering a mid-conversation request by rewriting a file and handing over a path moves the reading outside the interaction*), and a shipped assertion enforces it by failing on a path in the report body. #986 offers no argument against that ground; it offers a **symptom** — "the displayed result is truncated" — with the export as its remedy. The truncation has its own cause and its own fix, so the symptom is addressed at source and the export buys nothing the whole relay does not now provide. **What would reopen it, named so the next sitting inherits the reasoning instead of re-deciding it:** an owner who, having read an *untruncated* report, still cannot read it in a terminal — grounding the export in terminal limits rather than in truncation, which is evidence neither rejection has weighed. Delivery is stories 20.73 and 20.74.

> **Amended 2026-07-31 (triage, #988/#994/#995/#996/#997 — one decision, five issues) — the brief becomes a NAMED artifact with a visible lifecycle, G-ids become expanding shorthand, the gate gains an ITERATION LOOP and 2–3 CANDIDATE THESES, and a large selection may be proposed as k article-scoped groups.** The owner's verdict after the first real end-to-end run: *"It continuously displays information and requires the user to design everything from scratch in ordinary chat. That is the worst possible UX for this workflow."* Filed as a **bug** under the standing ruling that a rule-conformant surface bad for the user is an incorrect design. Five issues, one decision, because all five land at the selection → brief boundary and deciding them apart would decide the same thing several times differently. **The five are not one kind of change, and the distinction is what makes the decision safe.** #994/#996/#997 add **ergonomics** to a flow the owner already drives; #995/#988 add a **machine partition or re-reading of the owner's selection**, which is the act the second-proposer boundary polices. Both are admissible, but only for a stated reason: the boundary engages when something other than the owner **narrows** the candidate set, and at the brief gate the narrowing has already happened, by the owner, at selection. Composing several readings over an owner-selected set narrows nothing. **So the guard is not the boundary but the completeness invariant** — a **cover counted in placements** (*owner decision record — 2026-07-29 (terrain grouping and evidence model)*) — and it is checkable: a candidate thesis places every selected Strand or discloses the omission, and **the count check runs after composition**, since a composer that cannot omit in principle can still omit in fact. That check is the delivering stories' own falsifier. **#994 is consumer-side surfacing only.** That selection composes the brief is settled (*owner decision record — 2026-07-29 (terrain draft handoff)*) and is not reopened; what was missing is that the brief had no identity, no artifact and no stated lifecycle, so the owner could not tell when a brief began existing or how to return to one. The never-read-back rule does not transfer, and the difference is the point: a View is a rendering regenerated per invocation, while the brief is the owner's decision — re-opening it is the requirement, not a cache. **#996 relaxes a refusal that protected the right invariant with the wrong reach.** Selection refused group ids outright on the ground that a rendering is not an address. That ground survives exactly: a G-id may never be **recorded**, but it may be **typed** — expanding to its member L-ids at the screen that defined it, with mixed input composing by expand-then-set-arithmetic before the brief exists. Nothing downstream can distinguish the result from a selection typed member by member, which is the test. **A related recollection was checked against the record and NOT found:** a design in which the Brief screen recommends other Lessons from the same Group id. No such decision exists, and it is declined on the merits — keying a semantic act on one invocation's ephemeral presentation grouping. Sibling recommendation, if wanted, is computed from the **substrate** at recommendation time under the proposal contract; it may coincide with a group's membership but must not reference the grouping. **#997's semantics were already ratified and shipped** — a claim is pinned to its member set and recomposes when that set changes, a set change being a **gate event** rather than a refresh — so what is added is only the move: an edit-set option class in place of "go back to Screen 2", each recomposition pinned to its own set, prior compositions retained so the owner compares theses across set variants rather than remembering them. **#988 is the candidate-thesis machinery applied to a partition**, and stays deliberately separate from subdividing an oversized group on the *serving* screens: that is pre-selection presentation refinement bound by the terrain invariants, this is post-selection and bound by the proposal contract. Conflating them would put a machine partition **upstream** of the owner's selection, which is the one place the boundary does engage. **Grounding correction the delivering stories must not inherit:** #995 states that *"today's coherence-consultant pass already computes the material"* for candidate theses. It does not. The consultant computes a subject list and a complete unranked substitution pool and nothing else; the core-vs-adjacent split the owner observed is unpersisted judgment at the gate, and the consultant's rules are deliberately **not** a fixed procedure — an existing check grep-asserts that no procedure has been smuggled in. Candidate theses are therefore **new composition**, not a rendering of something already computed, and the story that builds them must not weaken that check to do it. **Observed good behavior preserved:** on five selected Strands the gate identified the stronger subset unprompted and offered narrowing as a ranked option with grounds and an overturn condition. Delivery is stories 20.75–20.79.

> **Amended 2026-07-31 (#889/#1018/#1031 — the offering gate DISCHARGED) — Journey similarity joins the OFFERED substrate set; co-tags remains the default.** The gate added 2026-07-29 held the model-judged substrate out of the offered set until one measurement run answered *do the machine-judged shared journey paths form groups the owner recognizes as one background?* **That run happened on 2026-07-31 and the owner verdicted PASS**, so this entry records a ratified conditional discharging — *"Pass → it joins the offered set"* was already the clause's own text — and not a new decision. **What was measured:** the `agents` member, 51 Strands; **10** machine-composed shared-path groups rendered in the approved compact form, each with its `in common:` line stating the shared arc; **48 of 51** Strands placed in a group and the remaining **3** accounted for explicitly in the two named residues; the permutation checked mechanically — count-in 51 = count-out 51, no drops, no duplicates, no invented ids. **The gate clause is left standing as written** rather than rewritten to match its outcome: a discharged condition is evidence about the design, and a clause edited into agreement with its own result leaves nothing to check the result against. **The verified constraints travel with the axis, unchanged** — presentation-only (sections gate nothing), all groups shown with no machine narrowing, section order a declared key and never a strength ranking, composed `in common:` lines marked machine-composed, and the `G` group-id kind declared on the surface that renders it. Two of those had no carrier on screen 2 until now: the composed listing printed `G` ids without saying what kind they are, and a judged grouping named neither its substrate nor its ordering rule on the reading surface, so both are stated there — on the judged path only, leaving the co-tag screen byte-identical, because a co-tag section's key is readable on every row and a judged one's is not. **The two residues stay DISTINCT and are never merged.** "No served journey arc" and "no shared path" answer different questions: the first Strand was never eligible for judgment, the second was judged and matched nothing, and folding them together would report a judgment that never happened. The measurement kept them apart (G11 vs G12) and the shipped fixtures exercise both. **Offering is not promoting:** a run naming no substrate still composes on co-tags, because the argument that admitted this axis is that the owner may now *choose* it, not that it groups better. **Two boundaries inherited rather than quietly closed:** the hub-wide Journey-coverage figure is **not** re-measured — the run measured 49 of 51 for one member, a different denominator, and said so — and **no ranking of groups** by strength, size or judged quality is admitted, since ranking is the far side of the second-proposer boundary and the compact all-groups form was adopted to remove exactly that judgment. **The unoffered set survives as an empty holding pen**, not deleted: the gate is a standing rule for model-judged substrates and the next one lands there until its own run passes. **What would reverse this:** groups that read as after-the-fact labels on a second member — the verdict rests on one corpus, which is the measurement's own stated reach. Delivery is story 20.82.

> **Amended 2026-07-31 (triage, #1040) — the View's FILENAME leaves #726's machine-key exemption; BOTH grounds of that exemption are falsified, each checkably.** The 2026-07-26 rename (#726, Alternative A, recorded above) cut at *"the boundary between what a reader reads and what a machine keys on"* and placed the View path among *"internal machine keys with no owner-facing reading."* Two days later a served line negates that premise directly: **owner-facing is a TYPE carried by the value rather than a property of which file it lives in** (*owner decision record — 2026-07-28 (one typed owner-surface seam carrying a positive register contract)*; consulted at the pin, coverage partial, **covered** on this fork). The View is the one artifact this flow writes for the owner to open — `docs/storage-architecture.md:130` classes it *"a human opens it to read; it is the deliverable"* — so the value is owner-facing whatever the path is, and the exemption misclassified it. **The second ground is discharged by fact, not by argument.** #726 recorded a blocker: *"the CAP-3 View path is a published-artifact location with a live file today (`<articles>/topic-map/<repo-key>/…`) and no discovery mechanism pointing at any other name. A future change renaming it must supply the migration."* That location **no longer exists**: the 2026-07-28 relocation (#874) moved the View into the writing-assistant repository's own output directory (`scripts/resolve-paths.py:326-330`, returning `terrain_repo_dir(root)`), which `.gitignore:36` excludes; no `topic-map/` directory remains anywhere on the machine, the sole live file is the gitignored one, and `TOPIC_MAP_VIEW_DIR` (`scripts/resolve-paths.py:269`) has **zero references** — dead since that relocation. So no migration is owed: the file is fully regenerated per invocation and never read back (CAP-1, unchanged), which is exactly the property #726 declined to rely on when the file sat in a published location and may rely on now that it does not. **What changes:** `VIEW_FILENAME` / `TOPIC_MAP_VIEW_BASENAME` become `terrain-view.md`, and the `topic-map-view` subcommand of `resolve-paths.py` becomes `terrain-view` — the subcommand travels with the filename because it is typed into an owner-facing procedure document (`skills/terrain/steps/screens.md:251,289`) and because a resolver whose verb and answer disagree manufactures the very drift #1039 records. The dead constant is deleted and the stale docstring at `scripts/resolve-paths.py:302-310` — which still asserts the pre-#874 path and the now-falsified reasoning — is corrected. **What does NOT change, answering the issue's open call:** `scripts/topic-map-directions.py` and `scripts/topic-map.py` keep their names, and the map's internal `topic`/`subtopic` grouping keys keep theirs. A script filename is not an artifact the owner opens, so #726's cut still holds for them; this amendment re-classifies **one member** of that exemption list rather than retiring the list. **Forward-only, inherited unchanged:** closed stories, merged issue titles and historical records keep their original wording; nothing is retrofitted. Delivery is story 20.85, sequenced **after** 20.83 and 20.84 so a mechanical sweep runs over a settled surface rather than two in-flight stories chasing a moving name.

> **Amended 2026-07-31 (triage, #1041) — the ruled subgroup-claim model is ADOPTED, triggered by TIGHTNESS DIFFERENTIAL and never by a member count.** The model itself was elaborated and ruled by the owner on 2026-07-30 in #980's closing comments and is not reopened here: the parent keeps its GroupClaim as the section header, each subgroup (`G10-1`, `G10-2`, …) carries its own machine-composed claim, and **the hierarchy renders visibly** — explicitly ruled, on the ground that showing only subgroup claims makes their relation to one another illegible while the Full Report's purpose is surveying the material as a whole. **What this amendment decides is only the trigger, and the served surface constrains it.** The remedy for an unreadable member was named at decline time as *"a SECOND NAVIGATION STEP — not a cap and not a re-tag,"* held rather than built *"because the trigger fired on size, and size is a proxy,"* with its watch trigger recorded as *"an owner in a real sitting unable to find the element they wanted — not a count, not a scroll length"* (*owner decision record — 2026-07-27 (no within-axis cap; a second navigation step)*; consulted at the pin, coverage partial, **covered** on this fork). The issue proposes *"groups above ~4 members"*, which is precisely the shape that line forbids. **So the trigger is the tightness differential:** a candidate group is subdivided when a trial subdivision yields subgroup claims measurably **tighter** than the parent's, and it is a leaf when they merely restate it. This promotes #980's own ratified tightness check — *"a subgroup claim that merely restates the parent's means the subset gained nothing"* — from a post-hoc verdict to the trigger, reading the same predicate earlier; it invents no constant and no cap. **The `~4 members` figure enters as CALIBRATION EVIDENCE and never as a threshold**, and nothing in this amendment licenses a member-count trigger later. **#980's cap decline stands untouched,** because a semantic trigger is not the cap it declined; and the recursive stop is #980's, unchanged — a subgroup whose claim degenerates into an enumeration splits further, one whose claim composes honestly is a leaf. **Invariants transfer to the leaves, stated so they are not rediscovered:** the cover is counted in **placements at the leaves** (`SPEC.md:277-289`), the count check runs **after** composition because a composer that cannot omit in principle can still omit in fact, subdivision is **presentation-only** with no rank, omit or hide, and the per-screen/per-pin display-id discipline (`scripts/terrain_members.py:395-402`) extends to `G<n>-<m>` — which confers no selection authority, exactly as `G<n>` does not. The existing `_subdivide_section` (`scripts/terrain_members.py:423`) is untouched: it subdivides deterministically by co-tag on the 20%-of-placements cap and **cannot** produce semantic groups, so the new layer sits beside it rather than replacing it. **The risk this decision carries is made executable rather than remembered.** Speculative claim composition costs work that may be discarded, on a model-judged substrate, and that cost was **not measured** at this gate. Following the precedent this file already set for an unmeasured premise, the measurement is story 20.86's **first acceptance criterion, with instruction to stop and re-triage if the cost is prohibitive at real corpus size** — in which case the degenerate-claim self-report (`scripts/terrain_members.py:634-650`, Story 20.67) becomes the fallback trigger, at the known price that it may not fire on an honestly-composed but overly broad claim, which is the reported G10 case. **A premise correction carried from the gate, so it is not re-derived:** in the reported run the sectioning cap was `max(3, int(107 × 0.2)) = 21` placements, so G10 (15) and G12 (13) were **both under it** — a cap-breach trigger would not have fired on the instance being reported, which is why the cap is not the trigger here. Delivery is stories 20.86 and 20.87.

> **Amended 2026-07-31 (triage, #1046) — the date cut becomes mechanical; the rule lives one level up.** SPEC-terrain's amendment history was split twice in five days (#829 on 2026-07-27, then this companion at its own ceiling on 2026-07-31), and the second split is why the shape was decided rather than repeated. The decision is **not terrain's**: amendment-history companions across every spec suite become their own budgeted file class, exempt from the spec byte ceiling and governed instead by an era-split threshold declared in `scripts/check-skill-budget.sh`. The reasoning, the measurement over all four companions, the discharged overturn test and the rejected per-block budget are recorded once in `specs/spec-writing-assistant/amendments.md` (2026-07-31, #1046) and are not restated here — this pointer exists so a reader of the terrain history learns that the rule governing this file is not written in it. The pointer block in `SPEC.md` carries the operative form. Delivery is story 20.88.

> **Amended 2026-07-31 (triage, #1043) — the thesis RECOMMENDATION becomes contract: required, axes declared at the point of use, overturn conditions stated, and nothing trimmed. What stays uncontracted is HOW the assessment is reached.** The owner asked the right question of a run they liked — *is this a mechanism, or luck?* — and the answer is neither, in a way that matters more than either: **two carriers disagree, and the false one is the field.** `THESIS_REQUIREMENTS[3]` forbids candidates being *"ENUMERATED, never **ranked-and-trimmed** — offering the strongest while discarding the rest"* (`scripts/terrain_theses.py:76-78`, restated `skills/terrain/steps/brief.md:103-104`); what it bans is ranking **coupled with trimming**. The block's `"ranked": False` (`:139`) asserts a strictly stronger property than the requirement behind it, so the observed run — rank, retain all three whole, disclose what would overturn it — **satisfied the requirement and contradicted the field**. The behaviour was also not unruled: this repository's `CLAUDE.md` §"Fork gates owe a ranked recommendation (#808)" binds any gate presenting options to carry a ranked recommendation with its reasoning and to state what would overturn it. **But that rule binds agents working in THIS repository, and the terrain gate ships as a plugin drawn in host repos**, which load their own `CLAUDE.md` and never this one — so the guarantee held only at home, which is the real gap and the one this amendment closes. **The boundary question the issue raised is settled by served position, not by good behaviour.** *owner decision record — 2026-07-28 (the second-proposer boundary)*: ranking is a proposal, but *"the test is not whether a machine COMPUTED something but whether what reached the owner is smaller than what exists"*; and *owner decision record — 2026-07-29 (terrain grouping and evidence model)*: *"the compact form still COMPUTES an ordering … Nothing is smaller."* A retained-whole ranking at a post-selection gate is admissible, and the narrowing has already been done by the owner at selection. **What is frozen is the arriving artifact, never the procedure** — which is the requirements list's own stated design, *"What must be TRUE of the candidates that arrive. Not how to arrive at them."* (`scripts/terrain_theses.py:65`). So a new requirement governs the recommendation: it is offered beside the candidates and never instead of any of them; machine-proposed, never machine-final; it names the axes it assessed on; it states what would overturn it; and rank confers no default — no candidate is trimmed, hidden, reordered away or abbreviated for ranking lower. **The three axes the owner named — redundancy, consistency, distinct Strand relationships — land as NAMED DEFAULTS in the skill prose and NOT as frozen requirements**, deliberately: `skills/terrain/steps/brief.md:107,231` declares the assessment *"not a fixed procedure"* on the stated ground that *"a narrow mechanism would keep reporting success for satisfying its own steps while failing what the owner wanted"*, and two shipped checks grep-assert that phrase (`scripts/check-terrain-consultant-inner.sh:95,162`; `scripts/check-terrain-theses-inner.sh:175`) — the #995 amendment above warns in terms that a delivering story *"must not weaken that check to do it"*. Freezing the axes would require re-scoping that guard, and this decision declines to pay that price for a guarantee the declaration already delivers in the form that matters at the gate. **Cost disclosed, and it is the option's own overturn condition:** declared axes are guaranteed to be STATED, not to be the SAME across runs, so recommendations stay legible per sitting and are not yet comparable across sittings. If cross-run comparability turns out to be the requirement, the axes must be frozen and the guard re-scoped to the consultant where it was earned — named now so it is not invented under time pressure later. **Two `ranked: False` fields exist and only one is wrong:** `scripts/topic-map-directions.py:372`, on the pre-selection members block (*"Unranked and complete"*, `:366`), is CORRECT and stays — pre-selection is exactly where the second-proposer ban does bind, and changing it would put a machine ranking upstream of the owner's selection. Delivery is story 20.89.

> **Amended 2026-07-31 (triage, #1044; coupled with #1045) — a SELECTED Strand's served journey arc travels with it into the brief and on to drafting. The anti-widening bound is UNCHANGED, because it was never what excluded the arc.** #1044 filed the exclusion as happening "by construction, twice" — in the brief composition's anti-widening ground and again in `thesis_candidates_block`'s `inputs`. **Traced at the code, that premise is half wrong, and the correction shrinks the fix from an invariant amendment to a four-line projection.** `matches` are the full element dicts and carry `journey` and `journey_cite` (`scripts/topic-map-directions.py:465`); the brief block already records `"members": members` with the completeness invariant following the set into drafting (`:494-497`); and `thesis_candidates_block`'s `inputs` is `list(members)`, so it receives whatever survives and is a downstream consequence rather than a second exclusion. **The whole drop is `_member_record` (`scripts/terrain_members.py:981-986`), an undocumented projection to four keys — `index`, `slug`, `gloss`, `cite` — whose docstring says only "as the brief records it" and gives no reason for what it leaves out.** So #1044's central argument is upheld for a better reason than it gives: the bound governs *scope* — unselected Strands' material — and a selected Strand's own served rendering was never outside it. Nothing in the invariant needed relaxing; a projection needed documenting. **The boundary that IS real is the owner's selection, and it lands exactly where the bound's own sentence puts it.** `_member_record` has three callers, and they are not alike: `subject` (`topic-map-directions.py:365`) and the brief's `members` (`:485`) carry POINTED-AT material and widen; `substitution_candidates` (`:370`) carries OFFERED material and does not. *"A composer at the gate cannot widen the scope past what the owner pointed at"* fails to exclude the arc from the first two and succeeds completely for the third. **The arithmetic agrees with the principle, which is why this is not a compromise:** the substitution pool is the COMPLETE unselected set, and on the 51-Strand member this same sitting handled (#1038) widening it would hang ~50 arc renderings on every persisted brief — an unbounded cost on material the owner did not select — against 3–5 arcs for the selection. **The carrying discipline is reused, not invented:** `journey_similarity_inputs` (`topic-map-directions.py:258-271`) already quotes each served arc verbatim with `served: false` and a `not_served_reason`, so a composer can tell *"no arc exists"* from *"no arc arrived"*; the widened record takes that shape. Register is bound by served position — *owner decision record — 2026-07-24 (topic-map register)* requires a Journey rendering to preserve the arc shape (before-position → what broke → after-position) and never collapse to rule-statement register — and it binds whatever travels, so it needs no restatement per hop. **Cost disclosed:** the brief artifact's schema grows, and the brief is persisted with a lifecycle and re-opened by `brief-open`, so an older `brief.json` must still open. **Overturn condition, recorded rather than resolved:** if the consultant's swap proposals genuinely need arcs — if *"which unselected Strand would strengthen this set"* is a judgment glosses cannot support — then the pool needs them too and the cost must be bounded some other way. That was not tested here. Delivery is stories 20.90 (the join) and 20.91 (the drafting handoff); **#1045 is PARKED**, not decided — see the next block.

> **Amended 2026-07-31 (triage, #1045) — the journey-incorporation REGISTER is parked behind a named trigger, and the park is the decision.** #1045 asks how an arc enters article prose — worked example inside a lesson's section, short story with the claim as its conclusion, or an independent paragraph beside the lesson — and states plainly that the owner holds no position. **The consult missed, and the miss is the finding:** the served surface constrains the register (*owner decision record — 2026-07-24 (topic-map register)*: a Journey rendering preserves the arc shape and never collapses to rule-statement register) and takes **no position** on which of the three serves an article's argument. So there is nothing to apply and a decision now would be taste recorded as contract. **What makes this a park rather than a stall is that the generating mechanism is named:** #1044's stories put the arcs where drafting can reach them, and the first draft composed with arcs available is the observation this decision waits on — not a count, not a date. This follows the discipline the co-tag second navigation step completed (*owner decision record — 2026-07-29 (terrain grouping and evidence model)*), where a remedy was named at decline time and adopted unchanged when its watch trigger fired, so nothing had to be invented under time pressure. **Recorded so it is not re-derived:** the three registers are not renderings of one choice — they produce structurally different articles — and the candidate mechanism, if one is ever wanted, is a per-thesis or per-section selection offered at the brief gate under the selection-not-freeform shape #995 established, with the served arc quoted at its pin whichever register is chosen. Whether the choice is per-article, per-section, or derivable from the adopted thesis's own shape is exactly what the dogfood run answers.

> **Amended 2026-07-31 (triage, #1048/#1049 — one decision, two issues) — the brief artifact persists the DECISION and recomposes the GATE INPUTS; and no owner act ever requires the owner to address it by path.** Two issues, one decision, because what the artifact IS determines what an owner-meaningful identity can be DERIVED FROM — deciding them apart would decide the same shape twice. **Measured on the real artifact before deciding, and the filed figure is understated:** `brief-adopted.json` is 161,693 B of which `consultant.substitution_candidates` is 130,493 B (**88.5%**) and the whole `consultant` block 132,572 B (89.9%), against ~10 KB of decision content (`members` 1,034 B, `candidate` 957 B, the brief string 498 B, pins, gaps). It is persisted **four times** in one run — `brief.json`, `brief-result.json`, `brief-adopted.json`, `brief-adopted-result.json` — for ~645 KB of a 1.6 MB workspace. **The ground is CLASS, never volume, and this is stated so the decision is not later defended on the wrong footing:** all terrain-runs together are 3.4 MB, so nothing here is a storage problem. The defect is that View-class content — *"a rendering regenerated per invocation; deleting it loses nothing, because it recomposes from the map"* — is persisted inside the one artifact defined as *"the owner's decision, which nothing can recompose"*, and that artifact is what is handed toward the canonical-draft gate. **The split is by RECOMPOSABILITY.** Persisted: `brief`, `adopted_claim`, `thesis`, `origin`, `members`, `pins`, `note`, `gaps`, `lifecycle`, and `iteration` — the composition chain being genuinely unrecomposable owner history. Not persisted: `consultant`, `candidate_theses.inputs`, `recomposition` — gate-time working inputs that live in the sitting's stdout and recompose from `map.json` at the recorded pin. **The premise was verified rather than assumed:** `map.json` IS retained in the same workspace (723,632 B), the pool is a deterministic projection of (map, tag, selected set) with all three recorded, and `docs/storage-architecture.md:147` records the retention stance as *"discharged by relocation, not by GC"* — so brief and map share a lifetime by construction and recomposition cannot silently fail. **A bounded residue (keeping the pool's ids at ~2% of cost) was considered and DECLINED on exactly that finding** — it hedges against a map pruned while briefs survive, which the relocation-not-GC stance makes a failure mode this design does not have; it becomes correct the moment that stance changes, which is this amendment's overturn condition. **Correction to #1048's own claim, so the delivering story is not surprised:** *"`brief-open` gains nothing to lose"* is not exact — `cmd_brief_open` ends in `print(json.dumps(payload))` and dumps the whole artifact today, so nothing it NEEDS is lost but its output changes and it must either recompose the blocks or stop printing them. **The #1049 half.** `docs/storage-architecture.md` has exactly two rows and **no row for the brief**, so its placement was a default rather than a decision: the run workspace was where Step 0 minted `$WS`, not the table's own test applied. The brief gains its row — run workspace, machine-read state — carrying the owner ruling as the row's own condition: *if placing the brief under the state dir means the owner must identify or type the filename at any point, that is a complete UX failure*. The file does not move; **every owner-facing route is closed by an act or a record instead** — `open the brief` (#1042, story 20.92), the plan pointer (#1050), and the gate ceasing to print the raw path as the owner's continuation surface. The served position is exact: *"a state stored only where a machine consumer reads it is not recorded, it is hidden — suppressing an item from an automated queue and making its state legible to a human are two obligations, and shipping the first makes the second feel done."* **Owner-meaningful identity is DERIVED, never stored** — date + member set + thesis first words, from fields the artifact already carries, in whatever lists briefs; no naming store is created. Delivery is story 20.93, which is **sequenced after story 20.90** (#1044): both change what the artifact persists, and 20.90 adds arc fields to `members`.

> **Amended 2026-07-31 (triage, #1051; coupled with #1050) — terrain's end ROUTES INTO draft-article's existing intent gate; it no longer relays a raw command.** The final screen relayed `stage0 <framework> <sources...>` with placeholders and asked in prose for "the framework and sources", at the exact moment the flow switches skills — pipeline vocabulary on the owner surface, no payload capture, and an absolute state-dir path embedded in a command offered to the owner. The gate that owns this fork already ships: `skills/draft-article/stages/stage0.md:61-64` asks by **intent label** against a closed set, giving a reason and a nearest fit rather than a bare list, with a captured payload (Story 19.13, #758). **So nothing is built — the boundary that currently skips that gate is routed into it.** Terrain grounds the recommendation from the adopted thesis's own shape; sources are asked **with the run's evidence state attached**, since a terrain-originated run arrives holding what no cold run has (this run's gap artifact had already declared all three Strands episodic-unrecorded with named recording targets, so the ask can name candidate sources instead of an unqualified `<sources...>`); the payload is captured; stage 0 runs with `--brief` wired from the artifact. The owner never sees the command, the path, or the word "framework". The served position covers the vocabulary half exactly — *owner decision record — 2026-07-28 (system-wide register rule)*: coining an owner-facing term obliges a reader-facing definition **in the same act**, and "framework" is stage vocabulary that was never owed one at this surface. **Entry-agnosticism does not bar this, and the direction is why:** the ratified property forbids *drafting* learning which producer ran; terrain calling draft-article's gate runs the opposite way and creates no obligation on drafting at all. **The uniformity clause at `skills/terrain/steps/brief.md:255-256` — *"nothing downstream can tell the two apart"* — has its scope narrowed to BEHAVIOUR explicitly**, because #1050 (recorded in `specs/spec-article-draft-pipeline/amendments.md`, same date) now has the plan record which producer ran: the *handoff* stays uniform, the *provenance record* does not, and [[uniformity-is-a-payoff-not-a-goal]] is the reason the two separate. **Interaction stated rather than discovered later:** story 20.91 (#1044) implements this same stage-0 handoff and its AC1 leaves the carrier deliberately open — this decision ANSWERS that open question, so 20.91 is re-read against it and story 20.95 lands first. Delivery is story 20.95.

> **Amended 2026-07-31 (triage, #1076) — a SERVED rendering renders whole in the View; the per-line display budget is scoped to lines the View COMPOSES.** The issue was filed against the Full Report, and that premise was already discharged: the report was unclipped earlier the same day (#986, above), before the run that produced the finding. What actually truncated is the **View**, and the finding survives the correction intact, because the View is the surface the spec's own *"the screen shows summaries; the path holds the whole"* clause points at — measured on that run, 103 of 107 rendered journey arcs ended mid-sentence in a 64 KB file. So this amendment resolves a contradiction *inside* this companion rather than adding a promise: the budget clause was clipping exactly the material the whole-path clause exists to hold. The discriminator is **composed versus served**. A composed line always has a shorter authored wording available, which is what `_fit`/`_fit_parts` give by authorship; a served rendering has none, because it is relocatable and never re-expressible, so the only sanctioned reduction is first-sentence-verbatim plus a pointer — the tier-1 index's rule — and a mid-text `…` is a third category the contract does not have. It is also the *worst* available cut for this material: an arc's shape is belief → break → new position, and truncation reliably keeps the belief and drops the break. Read with **UX defines correctness** (owner decision record — 2026-07-27 (terrain cold-reader verdict)), this is #986's reasoning applied to the surface #986's own clause pointed at. **Scope stated because the renderer is shared:** the exemption binds the View file, never the selection screens, which the size switch governs — the same row renderer feeds both, so fixing this at the renderer rather than at the surface would widen the decision into screens it did not decide. Alternatives considered and declined: *first-sentence-verbatim in the View* (leaves nothing holding the whole until the owner asks for a Full Report by group id, contradicting the clause this amendment exists to honour) and *disclose the reduction only* (records the defect instead of fixing it; the arithmetic — 2–4 sentences, ~80–120 words, the largest group under ~2k words — says there is no size problem to solve).
> **Amended 2026-07-31 (triage, #1077/#1078/#1079/#1080 — four issues, one decision) — the brief's CONTENT is contracted: an enumerated field set, an exclusion rule naming what never enters, one-fact-once, and provenance scoped to what it describes.** The four were triaged as one selection because each decides what the crossing artifact carries and none can be settled without the others. The prior clause (#994) settled that the brief *is* a named artifact and left its content to be chosen later; nothing chose it, and the measured result is the argument for closing it — a five-member brief at 41 KB across 21 top-level keys, containing rendered screen sentences as data, the step's own requirements and compliance self-assertion, a fill-in template sitting where a consumer reads values, map-internal working state carrying internal topic vocabulary across a boundary, a singular/plural key pair in which the singular described one member while both names read as "the selection", an `owner-authored` attestation over a machine-composed string, machine prose in the owner's free-text slot, a ratified field (harvest scope) absent entirely, and an `adopted` thesis whose candidates lived only in an unreferenced sibling file. **Choosing the field set is admissible and is this consumer's act:** the hub ratified the boundary property and explicitly not the schema, and the prior clause already says format is this consumer's to choose. **One premise was corrected before deciding, so no clause rests on it:** `candidates-pending` is correct *by design* — the command emits composition inputs and the agent composes — so what is contracted is not the pending state but that adoption may not outrun the record of what was adopted from. **The exclusion rule is the load-bearing half**, and its ground is the boundary property itself: drafting must not learn Terrain's rendering, invocation or lifetime, and an embedded screen sentence couples it to all three while being a second copy that drifts — one already had, the frozen line disagreeing with what rendered. Declined: *properties without field names* (matches the hub's non-ratification most literally, but a property check for "no rendering strings" is a judgment nothing can decide mechanically, where a key-set check is decidable) and *fix as bugs with no spec text* — the option that negates the shared premise, and the strongest of the three, since `skills/terrain/steps/brief.md` is a real operating contract; it was declined because the accretion happened *under* that contract, which governs the step and never the artifact's closure. **The list is expected to be amended** — the thesis model is days old — and that is the point: the contract is the closure, not permanence, so a field is added by amending this clause instead of by accretion nobody decided.

> **Amended 2026-07-31 (triage, #1075) — a section summary carries its CLAIM and declares any adopted SUBDIVISION; the 2026-07-31 (#1038) position that claims do not ride along with summaries is REVERSED on its own ground.** That position was recorded with a reason — *unclipped composer prose is unbounded screen height* — so the reversal is owed a measurement rather than a preference, and the measurement is against the run that produced the finding: **20 composed claims, median 173 characters, longest 215, 3536 total**, roughly 25 rendered lines against a summary of about thirty. Most claims are already one sentence. What the measurement shows is that height is bounded by group **count** — which the ratified 20%-of-placements sectioning cap already bounds — and was never bounded by prose length; the original position generalised "unclipped" into "unbounded" without the count bound in view. **The bound is now stated rather than assumed**, because claim length genuinely has no cap and #976 forbids clipping a claim: whole where it fits one line, otherwise first sentence verbatim plus a pointer to the whole — the same sanctioned reduction this companion states for served renderings in the #1076 amendment above, applied to composed prose for the same reason, and a mid-sentence ellipsis remains unavailable on every surface. **What the reversal buys is the surface's purpose:** with only a co-tag and a count under each heading, the summary's information content over the tag name *is* the count, so every judgment required opening the View and the judgment screen was a table of contents wearing a judgment surface's introduction. The subdivision half needed no reversal and is added beside it: a screen that defines the `G<n>-<m>` id family while exhibiting no member of it leaves the owner facing a parent's fifteen members exactly as before the mechanism shipped, and a per-group subgroup count is a count rather than prose, so the height argument never reached it. **Recorded because the announcement is the check-able pairing:** the observed screen opened by announcing that every `in common:` line is machine-composed at render time, above twenty headings carrying none — so a summary that renders no claims does not announce them, and the two move together under any later amendment. **Reclassification noted:** #1075 was triaged story-sized on the ground that rendering changed no invariant; the altitude re-check at implementation found the summary's contents enumerated in this companion ("derived title, member ids, counts"), which made it spec-touching, and it was routed here in the same sitting rather than landed as a story against unamended text.

> **Amended 2026-07-31 (triage, #1090) — a per-invocation judged surface carries a JUDGE PIN, declared by the agent and stated absent when it is not.** The hub ratified the pin vocabulary and the scope; this is the consumer's emitting half, and the only question this repository owned was where the value comes from. It cannot come from the composing code: `topic-map-directions.py` emits `pins: {terrain, hub, destination}` from deterministic Python that has no way to know which model served the judgment it is recording. So the judge is **declared at the boundary that knows it** — the agent passes it — which is declare-authority-never-infer-it arriving at its own site, and **absence is recorded as absence** in the same three-valued shape a served arc already uses, never as a missing key and never as a guess. **Requiring it outright was considered and declined for now**, on the trade the issue itself frames: a hard refusal turns every skill-wiring gap into a broken flow while the emitting half is still reaching its callers. That decision carries its own reopen trigger rather than resting on optimism — **if callers routinely omit the judge the field measures nothing**, which is the exact failure the pin exists to prevent, and the honest response then is to make it required. Recorded with it, because a pin nobody acts on is one of the two ways this loop stays open: the hub's companion tripwire re-runs a held measurement specimen on a change of serving judge and puts the diff before the owner. The pin makes a judge change observable; the tripwire makes it consequential. **What is deliberately not pinned is ratified hub artifacts** — a ratified `gloss:` is frozen by write-once plus human approval, so whatever model proposed it the owner ratified it, and pinning there would record a consequence-free fact while inviting the false inference that a ratified line is contingent on its proposing judge.

> **Amended 2026-08-01 (triage, #1102; coupled with #1103) — terrain's screens emit through the GATE CARRIER, and the "name candidate sources" licence is SCOPED to informing a default.** Two clauses of this spec's own surface are corrected by one observation. **First, the screens.** `skills/terrain/steps/screens.md:30` reads *"Present the validated payload **in-conversation** under the owner-facing proposal contract"* — and that instruction is why screen 1 and screen 2 were outside 20.103's conversion and why the 2026-08-01 sitting met five prose gates in a row. The payload machinery was never missing here: `skills/terrain/steps/screens.md:16-19` already derives `topic-map.payload.json` and validates it through the shipped validator. What was missing is a statement that the *rendering* quotes it. Per the same-date carrier amendment in `specs/spec-writing-assistant/amendments.md` (#1102), screens 1 and 2 emit a payload carrying `render:`, and the instruction becomes **quote the payload**. **Screen 2 declares `control: "block"` and this is a boundary, not a defeat:** the Strand pick offers roughly fifty selectable units against a host control admitting four, so the conforming form is the enumerated decision block the carrier amendment defines — composed from the payload, carrying the banner and the explicit reply line. **The second-navigation-step remedy is NOT adopted here**, and the reason is recorded so it is not re-litigated: *(owner decision record — 2026-07-27 (within-topic cap))* declined it with its watch trigger stated as *"an owner in a real sitting unable to find the element they wanted in a served member's listing — not a count, not a scroll length"*, and #1102 reports something else entirely — that no control appeared at all. The trigger has **not** fired, and discharging it on a display complaint it was not reserved for is the shape *(owner decision record — 2026-07-23 (a demand trigger's count is a proxy for observed need))* names, where an override with no failed sitting behind it is the violation and looks near-identical in the record. **Second, the sources licence.** The 2026-07-31 (#1051) amendment above established that a terrain-originated run *"arrives holding what no cold run has"* and licensed the handoff to *"name candidate sources instead of an unqualified `<sources...>`"*. That reasoning stands and is not reversed; what it under-specified is the **answer format**, and the implementation read "name candidates" as "have the owner type these paths back". The licence is **scoped**: named candidates inform the **default scope** and appear as the evidence for it, and they never become the format in which the owner answers. The evidence-state attachment the amendment ratified is preserved in full — it is what makes the default non-arbitrary — while the granularity question moves to `specs/spec-article-draft-pipeline/amendments.md` (same date, #1103), which is where the stage-0 gate is governed. Delivery is stories 20.108 and 20.109.

> **Amended 2026-08-01 (triage, #1115/#1116) — the View's human budget extends to GROUP PRESENTATION, and every element dropped from a surface names its receiver in the same clause.** CAP-3 already rules that *"the screen shows summaries; the path holds the whole"* (`presentation.md:309-312`) and that *"the View is a human surface, so it is budgeted"* — under which *"line-granular pointers are machine provenance"* and evidence renders aggregated with the remainder disclosed as a count, never silently truncated (`presentation.md:~464-470`). That budget was written for the **evidence summary** and never reached the per-Strand rendering inside group presentation, which is why the 2026-08-01 View for tag `agents` carried **110 annotation lines whose 40-character hub pin was identical on every one** — the whole View is served at one hub commit by construction, so only the `LESSONS.md` line number varied, and the `claim and reasoning both recorded` clause was uniform across all 110. Repetition at that ratio is the log-file failure the budget clause names, arriving on the surface the clause did not cover. **What group presentation carries is now closed: Group ID, Strand ID, gloss, journey — and nothing else.** The per-Strand annotation line is removed from the screen and from the Full Report alike. **Each removal names its receiver in this same clause**, because deleting content from one surface while assuming another carries it is the drop this rule exists to prevent: (1) the **shared hub pin is stated once** in the View header, where it already appears, and a **single end-of-report section** maps Strand index → `LESSONS.md:<line>`; a per-Strand pin returns only if a View can ever be served at mixed pins, which it cannot today. (2) The **`also in:` co-tag list** is group-structure information the group heading already carries; where it merely restates the parent group's axis it is dropped, and where it names tags beyond the group's it moves to that same end-of-report section — never inline. (3) **Screen 2's instruction boilerplate** — the block from *"The complete rendering —"* through *"Selection is always by Strand index."* — is stated **once, in the View**, never on the screen: it is reference material standing between the owner and the groups they are choosing among, which inverts *"the summary is a judgment surface, not a table of contents"* (`presentation.md:313-316`, #1075). **Two additions make the Display view self-sufficient rather than merely shorter**, since the defect the removals leave behind is that no surface maps groups to members compactly: **group headings carry their Strand indexes** (`## G3 — also architecture + method — 2 Strands: L1, L2`; the exact format is the composer's), and **SubGroups render under their parent group** in the Display view with their id and claim line, where today they are a bare count annotation (`G10 — … (15, 5 subgroups)`) reachable only through the View. Non-member disclosure is preserved throughout: a reader can tell that provenance was moved rather than discarded, because the end-of-report section is where it went. **Nothing here widens what the map proposes** — the no-second-proposer boundary and the completeness invariant are untouched; this clause governs rendering only. Delivery is stories 20.113–20.114.

> **Amended 2026-08-01 (triage, #1113/#1112; the paths half is #1117, recorded in `specs/spec-writing-assistant/amendments.md`) — the brief's TRANSITIONS are stated on the surface, and the handoff discloses the owner decisions still to come.** Two findings from the 2026-08-01 sitting, one surface. **The transition (#1113).** The owner reports *"Because there is no explicit brief-creation command, I am never fully confident that the workflow has actually transitioned from Terrain into Brief correctly."* A **named step identity already ships** — `BRIEF_STEP_NAME = "Step 3 — compose the brief"`, whose purpose is recorded in the code as *"the act the owner can refer to, instead of 'the message above'"* (`scripts/terrain_text.py:60,69-72`, CAP-3 2026-07-31) — and the lifecycle line deliberately renders the whole machine with the current state legible rather than the state alone (`terrain_text.py:75-84`). So the gap is neither identity nor state: **it is that no transition is ANNOUNCED at the moment it happens.** The surface therefore emits a **fixed transition banner at Step 3 entry and again at each lifecycle transition** (`composed → inspected → adopted`), quoting machine state rather than narrating it: the step id, the run workspace, the artifact path — rendered whole per the owner-surface seam instance above — and the current lifecycle state read from the artifact. *"Selection recorded. Entering Step 3."* is narration, and narration is precisely the register the owner reports being unable to distinguish from a machine fact. **A named brief sub-command is DECLINED, with its reason recorded so it is not reopened as an oversight:** #1113 leaves the question open and states the requirement as the owner being able to *confirm* the transition, not trigger it; nothing in the run suggests the inline composition after a free-text selection was wrong, only that it was unannounced, and adding a command step to a deliberately free-text selection path spends the flow's directness to buy evidence a banner already supplies. **Reopen trigger:** a sitting where the banner is present and the owner still cannot confirm the transition. **The pending decisions (#1112).** The owner expected the brief stage to ask for article, paragraph and figure structure, and read their absence as a possibly-skipped gate. **No gate was skipped**, and the placement is intended: the narrative-structure choice is stage 3 and cannot run earlier because its inputs are the harvested evidence kinds (`skills/draft-article/stages/stage3.md:42-48`, CAP-4, story 18.26/#503 — *"a framework's narrative structure is owner editorial intent per artifact, never a hardened tool default"*); the visual-set plan is likewise stage 3 (SPEC-article-visuals CAP-2a); and **paragraph structure has no owner gate anywhere by design**, because frameworks bind section *obligations*, *"not its literal heading structure"* (`stage3.md:40`). The defect is disclosure: nothing at the handoff says which decisions remain and where they will be asked, so an owner who expected one at the brief can only conclude the pipeline decided it silently. **The handoff summary therefore carries the map of owner decisions still pending, each named with its stage** — intent (stage 0), sources (stage 0), gap interview (stage 2), narrative structure (stage 3), visual set (stage 3) — and states that paragraph structure is never asked, with its reason. This is **non-member disclosure** (*a reader can tell that an absent item was considered and excluded rather than dropped*) applied to decisions rather than to content: the absence of a structure ask at the brief must read as *later*, and the one that is never asked must read as *never, and here is why*. **The map is derived from the stages that own the gates, never hand-listed** — a hardcoded list is a conformance copy with no precedence rule, and it would drift the first time a gate moves. Delivery is stories 20.116–20.117.

> **Amended 2026-08-01 (triage, #1114) — terrain's gate surfaces are subject to the ASKING-IS-EMITTING constraint; the operative clause is in `specs/spec-article-draft-pipeline/amendments.md` (2026-08-01, #1114) and is not restated here.** What this pointer records is terrain's own instance: the **thesis gate** was the surface that reached the owner with **no payload in `presented-payloads.jsonl` at all** on the 2026-08-01 run, so 20.108's headline ("terrain screens and the thesis gate emit through the carrier") did not hold in practice, and terrain's screen composers lose the free-composition path along with every other gate surface. The screens already emit `render` (`scripts/terrain_screens.py:328,392,438`); the thesis gate had no emission site to fix. Delivery is story 20.118.

> **Amended 2026-08-01 (triage, #1118) — THE ADOPT RECORD ATTESTS TO THE OWNER'S ANSWER, reusing the vocabulary the selection path already emits; the lifecycle surface renders the map AND the traversal.** The filing's original claim — that candidate B was adopted without the owner naming it — was **withdrawn**: the owner corrected the transcript and the adoption flow worked correctly. What survives is why an incomplete transcript was enough to make a correct adoption look like a defect. **The record cannot arbitrate.** With the owner's answer "B", the contract puts B's thesis into `answer-adopt.json`'s `claim` by design, and the resulting record is **byte-identical to what a silent, machine-initiated adoption would have produced**: nothing in `answer-adopt.json` or `brief.json` records *that the owner named B*, only the adopted text. That is the shape *a gate-updated record must not carry an existence claim* forbids, and it is why the post-hoc investigation reached for a transcript at all. **The remedy is REUSE, not invention, and this is the load-bearing correction to the filing:** `scripts/topic-map-directions.py:542` already emits `"origin": "adopted-index" | "adopted-index-set"`, and `:705-706` already emits `"provenance": "owner-authored", "origin": "adopted-candidate", "candidate": c` — the exact vocabulary #1118 proposes, shipped on the **selection** path and never carried to the **adopt** path. So `answer-adopt.json` carries the owner's **utterance verbatim**, its `provenance`, its `origin` (`adopted-candidate` vs `owner-freetext`), and the resolved candidate, in the fields that already exist. A post-hoc audit of `brief.json` alone then distinguishes an owner-named adoption from a defect, which is the whole ask. **Two record oddities are corrected under the same clause**, because both fuse differently-sourced text into one attested string: `note` no longer re-attributes the **earlier selection gate's** free text (`"create brief using G4 members"`) to the adoption answer, and `brief` no longer concatenates the adopted thesis with that selection note — thesis and selection note are separate fields, since a single attested string composed of two provenances can be quoted truthfully as neither. This is the same discipline as *the brief records the adopted claim together with the members it was composed from*: a derived expression carries what it was derived from. **The lifecycle renders BOTH, and the reason the obvious fix is refused is recorded.** The filing offers a choice — record the traversal, or render `lifecycle.history` instead of the template. Rendering history *instead* would reverse a deliberate decision without engaging it: `scripts/terrain_text.py:75-84` renders the whole machine with the current state bracketed precisely so the owner is *"never the current state alone, because one word out of three tells the owner nothing about what comes next"*, and history is the current state plus its past — still silent on what comes next. **Verified against shipped code, the line is also not the bare template the filing reports:** it renders `Lifecycle: composed → inspected → [adopted] — now: adopted.` The residual defect is real and narrower: **bracketing marks where the owner IS, never where they HAVE BEEN**, so `inspected` — never entered on this run — appears with nothing saying so. The surface therefore keeps the forward-looking map **and adds the recorded traversal**, so the display and the record cannot disagree about which states happened, because both are rendered. **Verified correct on the run and recorded so it is not re-litigated:** G4 expands to L12, L52, L55, L74, L101 consistently across `answer-adopt.json` and `brief.json`; all five members' glosses, cites and journey arcs are present, served at the run's recorded hub and terrain pins (locators held in the private provenance store, not restated here); the five `needs_recording` blocks are intact. Delivery is stories 20.120–20.121.
